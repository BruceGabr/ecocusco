# Dependencias de producción: es lo único que se instala en la imagen Docker
# que Render construye, así que aquí no entra nada de pruebas.
#
# `passlib` se movió a requirements-dev.txt: la aplicación usa `bcrypt`
# directamente y solo un test lo importaba.
fastapi>=0.115.0
uvicorn>=0.30.0
python-dotenv>=1.0.0
email-validator>=2.2.0
pydantic>=2.0.0
psycopg[binary]>=3.2.0
PyJWT>=2.10.0
bcrypt>=4.0.1,<5.0.0
